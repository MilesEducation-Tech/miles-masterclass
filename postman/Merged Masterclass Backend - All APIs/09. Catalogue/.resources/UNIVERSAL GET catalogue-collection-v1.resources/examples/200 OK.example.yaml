$kind: http-example
request:
  url: "{{base_url}}/api/v1/catalogue/collection/{{collection_id}}/"
  method: GET
  headers:
    Accept: application/json
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "status": "ok",
        "result": "<async_cache_get(key, request=request)>"
      }
order: 1000
