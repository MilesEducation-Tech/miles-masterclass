$kind: http-example
request:
  url: "{{base_url}}/api/v1/catalogue/variant/{{variant_id}}/"
  method: GET
  headers:
    Accept: application/json
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "status": "ok",
        "result": {
          "<**payload>": "<merged in>",
          "posts": "<posts>"
        }
      }
order: 1000
