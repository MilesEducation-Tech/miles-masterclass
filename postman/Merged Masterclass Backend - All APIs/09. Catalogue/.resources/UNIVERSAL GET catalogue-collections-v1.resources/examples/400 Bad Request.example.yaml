$kind: http-example
request:
  url: "{{base_url}}/api/v1/catalogue/collections/"
  method: GET
  headers:
    Accept: application/json
response:
  statusCode: 400
  statusText: Bad Request
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "status": "error",
        "reason": "invalid_query",
        "errors": "<pydantic_error_list(exc)>"
      }
order: 2000
