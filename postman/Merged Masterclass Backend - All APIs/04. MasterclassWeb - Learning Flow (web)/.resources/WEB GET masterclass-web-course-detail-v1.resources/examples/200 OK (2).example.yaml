$kind: http-example
request:
  url: "{{base_url}}/api/v1/masterclass-web/course-detail/{{course_id}}/"
  method: GET
  headers:
    Accept: application/json
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "status": true,
        "course_details": "<_web_stamp_nasba_boilerplate(user_cached)>"
      }
order: 2000
