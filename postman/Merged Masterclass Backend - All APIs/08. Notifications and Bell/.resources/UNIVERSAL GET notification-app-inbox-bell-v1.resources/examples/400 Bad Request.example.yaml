$kind: http-example
request:
  url: "{{base_url}}/api/v1/notification/app-inbox-bell/"
  method: GET
  headers:
    Accept: application/json
response:
  statusCode: 400
  statusText: Bad Request
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "status": "Failed",
        "errors": "<query.errors>"
      }
order: 1000
