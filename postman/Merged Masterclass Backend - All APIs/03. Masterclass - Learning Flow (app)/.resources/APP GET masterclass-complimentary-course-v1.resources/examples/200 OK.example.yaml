$kind: http-example
request:
  url: "{{base_url}}/api/v1/masterclass/complimentary-course/"
  method: GET
  headers:
    Accept: application/json
  queryParams:
    - key: course_type
      value: ""
      disabled: true
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "<body>": "<_envelope('Complimentary courses returned successfully!', data, pagination_data=pagination_data, extra={'company_name': company_name})>"
      }
order: 1000
