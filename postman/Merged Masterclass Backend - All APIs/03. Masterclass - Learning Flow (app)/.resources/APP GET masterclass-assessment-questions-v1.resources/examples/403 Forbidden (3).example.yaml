$kind: http-example
request:
  url: "{{base_url}}/api/v1/masterclass/{{course_id}}/assessment/questions/"
  method: GET
  headers:
    Accept: application/json
response:
  statusCode: 403
  statusText: Forbidden
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "status": "error",
        "reason": "chapters_not_complete"
      }
order: 4000
