$kind: http-example
request:
  url: "{{base_url}}/api/v1/masterclass/{{course_id}}/assessment/questions/"
  method: GET
  headers:
    Accept: application/json
response:
  statusCode: 500
  statusText: Internal Server Error
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "status": "error",
        "message": "Unable to fetch the assessment."
      }
order: 8000
