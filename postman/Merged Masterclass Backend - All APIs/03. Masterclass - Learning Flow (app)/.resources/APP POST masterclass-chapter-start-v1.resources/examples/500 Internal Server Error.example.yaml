$kind: http-example
request:
  url: "{{base_url}}/api/v1/masterclass/chapter/{{chapter_id}}/start/"
  method: POST
  headers:
    Accept: application/json
response:
  statusCode: 500
  statusText: Internal Server Error
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "status": "error",
        "message": "Unable to start the chapter."
      }
order: 7000
