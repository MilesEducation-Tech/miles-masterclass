$kind: http-example
request:
  url: "{{base_url}}/app-api/v1/events/all-bookings/"
  method: GET
  headers:
    Accept: application/json
  queryParams:
    - key: window
      value: ""
      disabled: true
response:
  statusCode: 403
  statusText: Forbidden
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "detail": "Authentication credentials were not provided."
      }
order: 3000
