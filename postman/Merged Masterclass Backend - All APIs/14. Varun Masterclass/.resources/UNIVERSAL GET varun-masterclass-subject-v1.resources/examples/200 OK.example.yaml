$kind: http-example
request:
  url: "{{base_url}}/api/v1/varun-masterclass/subject/{{subject_id}}/"
  method: GET
  headers:
    Accept: application/json
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "video": "<promo_payload>",
        "subject": "<subject_payload>"
      }
order: 1000
