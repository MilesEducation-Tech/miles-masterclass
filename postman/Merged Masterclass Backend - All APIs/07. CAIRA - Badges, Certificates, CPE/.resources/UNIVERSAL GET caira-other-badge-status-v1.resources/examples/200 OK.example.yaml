$kind: http-example
request:
  url: "{{base_url}}/api/v1/caira/other-badge-status/"
  method: GET
  headers:
    Accept: application/json
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "<body>": "<async_cache_get(cache_key, request=request)>"
      }
order: 1000
