$kind: http-example
request:
  url: "{{base_url}}/api/v1/account/questions/"
  method: GET
  headers:
    Accept: application/json
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "Questions": "<_format_questions(unformatted_data, options_map)>"
      }
order: 1000
