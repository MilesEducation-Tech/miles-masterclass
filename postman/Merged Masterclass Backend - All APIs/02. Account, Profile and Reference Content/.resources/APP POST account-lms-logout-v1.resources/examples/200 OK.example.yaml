$kind: http-example
request:
  url: "{{base_url}}/app-api/v1/account/lms-logout/{{course}}/"
  method: POST
  headers:
    Accept: application/json
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "message": "Logged out of the LMS.",
        "status": "Success"
      }
order: 1000
