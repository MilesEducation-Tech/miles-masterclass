$kind: http-example
request:
  url: "{{base_url}}/app-api/v1/account/lms-login/{{course}}/"
  method: POST
  headers:
    Accept: application/json
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "access_token": "",
        "uid": ""
      }
response:
  statusCode: 409
  statusText: Conflict
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "message": "This LMS account is already linked to a different Miles account.",
        "status": "Failed"
      }
order: 7000
