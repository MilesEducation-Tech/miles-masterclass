import { Component } from '@angular/core';
import { RouterOutlet } from '@angular/router';

@Component({
  selector: 'app-plain-layout',
  imports: [RouterOutlet],
  templateUrl: './plain-layout.html',
  styleUrl: './plain-layout.css',
})
export class PlainLayout {}
