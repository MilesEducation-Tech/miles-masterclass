// Replaced by blog-home / blog-all in the redesign. This folder can be
// deleted — it's no longer referenced by any route. (Left as an empty module
// because the sandbox couldn't unlink the original files.)
export {};
