@AGENTS.md

<!--
  Gemini CLI loads GEMINI.md by default; the import above pulls in AGENTS.md, the single source of truth
  for how to work in this repo (structure, modernization standards, git workflow). Don't add rules here:
  add them to AGENTS.md so every tool (Claude, Cursor, Copilot, Codex, Gemini) sees the same thing.
-->
