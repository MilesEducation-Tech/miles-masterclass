# CAIRA gap register

Frontend surfaces with no counterpart in the CAIRA Web API (50 endpoints,
`CAIRA_Web_API_Reference.md`). Each entry names what the UI needs, what the backend would have to
add, and what the code does in the meantime.

Nothing here is broken-on-purpose: every listed surface either renders an empty state or hides the
affected control. None points at a dead URL.

Last updated: P-P0 (CAIRA LMS parity audit — registry extended, G-23 closed, G-34/35/36 added) · P8 QR login shipped, G-04 narrowed to the decrypt step · P-P5 / P-P6 (levels, badges, webinars + async registration bound; G-23 bound, G-37 added) · CPE tracker reshaped to CAIRA and wired · P-P3 chapter player bound (#6/#18), G-39 added · CPE mode forced on for every chapter pending the enrollment flag.

## Blocking implementation

| #    | Gap                                                     | Blocks                 | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| ---- | ------------------------------------------------------- | ---------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| G-01 | `CAIRAMasterclassQuizQuestionSerializer` field list     | P5 quiz (#7, #9)       | Explicitly outside the reference's scope. Needs a UAT capture before the quiz can be typed.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| G-02 | `CAIRAMasterclassFeedbackQuestionSerializer` field list | P5 feedback (#12)      | Same.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| G-03 | `_build_full_course_progress` key set                   | P4 progress (#17, #18) | Same. Also `get_items()`'s single-vs-bulk wrapper key in `Masterclass/schemas.py`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| G-04 | QR crypto contract — **resolved from a working client** | —                      | `account/qr_login/crypto.py` is still out of the reference's scope, but every parameter now comes from a client that talked to this same module (identified by the matching `miles-qr-login-*` salt/info): ECDH **P-256**, keys as **raw uncompressed EC point / base64** (not SPKI), HKDF-SHA256 with salt `miles-qr-login-salt-v1` and info `miles-qr-login-aesgcm-v1`, AES-256-GCM with the tag appended. All pinned by tests. **Still open:** the decrypted plaintext's shape — #37 documents `{access_token, refresh_token, user_id}`, a sibling client documents `{user_id, session_id, token_id}`. Only the first is usable; the second is references with no endpoint to redeem them. `parseSession` reads the documented shape and reports the keys it actually got. Unverifiable until one real phone scan. Also still unknown, not blocking: whether the dash in `"4324-3456"` matters to `verify_pin`. |

## Auth and profile

| #    | Gap                                                                                             | Where it shows                                                                       | Current behaviour                                                                                                                               |
| ---- | ----------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------- |
| G-05 | **No OTP throttling.** #34 and #35 are both unthrottled server-side; only #33 has a rate limit. | Login                                                                                | A 30 s client-side resend cooldown, which stops double-taps but not abuse. Backend ask.                                                         |
| G-06 | No email-OTP login route.                                                                       | Login, Email tab                                                                     | The Email tab is email **+ password** (#33). `otp/generate` / `otp/validate` exist but verify an address post-login; they are not a login path. |
| G-07 | No password reset.                                                                              | `/auth/forget-password`                                                              | Route exists, component is an empty stub. #33 has no reset endpoint and the phone path has no password.                                         |
| G-08 | No signup.                                                                                      | `/auth/signup`                                                                       | Route exists, component is an empty stub. Accounts are created in the Miles One app.                                                            |
| G-09 | `onboarding` is unusable as a completeness signal — #33 hardcodes it `true`.                    | `isExistingUserGuard`                                                                | Completeness derives from `v2/status`'s `mo_first_name` / `mo_full_name` / `mo_email` instead.                                                  |
| G-10 | Profile reference data: **state boards, professional courses, companies, sectors, job roles**.  | `/auth/profile` (5 of 13 fields), profile-completion dialog, firm-sponsorship dialog | Fields render with empty option lists and are excluded from the `v2/update` body. Five reference-data endpoints needed.                         |
| G-11 | Place/location autocomplete.                                                                    | `/auth/profile`                                                                      | The location field is free text.                                                                                                                |
| G-12 | No `is_beta_access` / trial flag on the user.                                                   | Footer `hasTrailAccess`                                                              | Held `false`.                                                                                                                                   |
| G-13 | `#35`'s documented response omits `refresh_token` while `#33`'s includes it.                    | Token lifecycle                                                                      | Read defensively; an absent refresh token means the session ends when the access token expires. Needs confirming against a live OTP login.      |

## Entitlement and commerce

| #    | Gap                                                                                                                            | Where it shows                                                   | Current behaviour                                                                        |
| ---- | ------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------- | ---------------------------------------------------------------------------------------- |
| G-14 | **No plan or subscription model at all.** Access is `User.enrolled` tag membership, surfaced per-course as `course_is_locked`. | `activePlanGuard`, `Auth.currentPlan`, header, engagement dialog | `currentPlan` stays `null`. Replacing it with an entitlement check is its own decision.  |
| G-15 | Subscriptions, cart, coupons, plans, orders, invoices.                                                                         | `features/payment/**`                                            | Placeholders, empty states. Not a rebind — `commerce/` models one-time product purchase. |
| G-16 | Partner Platform: networks, firms, sub-companies, partner RBAC, coupon tracker.                                                | `admin/partner-platform/**`                                      | Placeholders. Zero backend counterpart.                                                  |
| G-17 | Partner-code redemption, firm sponsorship.                                                                                     | `PartnerCode`, firm-sponsorship dialog                           | Stubbed.                                                                                 |

## Content and discovery

| #        | Gap                                                                                                                                                                                            | Where it shows                    | Current behaviour                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| -------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| G-18     | Micro-learning as a CPE course type.                                                                                                                                                           | `offerings/micro-learning/**`     | No chapters, quiz, assessment or per-course CPE in CAIRA. Empty state.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| G-19     | Podcast as a CPE course type.                                                                                                                                                                  | `offerings/podcast/**`            | Same.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| G-20     | Text search and suggestions.                                                                                                                                                                   | Global search                     | No search endpoint anywhere in the backend. Service deleted.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| G-21     | Instructor **list** (detail-only exists at #16).                                                                                                                                               | `library/instructor-library`      | Empty state.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| G-22     | Recommendations, because-you-watched, complimentary.                                                                                                                                           | 4 of the 8 masterclass home feeds | Empty rails.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| ~~G-23~~ | ~~Webinar **registration**~~ — **CLOSED.** The API reference omits it, but `registerV4/` (POST, async) plus `registerV4/:attemptId/status/` are live and are what the shipped CAIRA LMS calls. | `webinar-registration-form`       | **Bound** in P-P6 — `Webinars.register()` posts L3 then polls L4 (12 attempts, 2 s). Components not yet rewired.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| G-24     | Public (crawlable) course detail — #4 requires a JWT.                                                                                                                                          | SEO on course pages               | Anonymous crawlers get the shell plus the Supabase `seo_pages` row. A public variant of #4 would fix it.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| G-29     | Courses **by instructor**. #16 returns the profile only.                                                                                                                                       | `/instructor/:id` — all 3 tabs    | Empty states. The only instructor→courses data CAIRA has is `instructor_related_courses` inside a _course's_ #4 payload, which needs a course you already know.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| G-30     | **CPE vs Preview mode.** No mode flag, no mode toggle endpoint.                                                                                                                                | Course hero mode pill + reset     | `cpe_mode_details` is pinned `null`, so the mode pill and its toggle never render, and the "Switch to CPE Mode" dialog was **removed** — it could never fire correctly. **Every chapter now runs in CPE mode** (`CPE_MODE_DEFAULT` in `ChapterProgress`): seek lock, quiz hand-off and the next-chapter gate are all enforced, because the alternative — preview mode — silently drops every compliance rule the credential depends on. `chapterWiseDetails` is rebuilt per chapter from `play_history.is_completed` since CAIRA sends no such list. **Awaiting a mode flag on the enrollment API**; `ChapterProgress.setCpeMode()` is the single seam, called from #6's response. |
| G-31     | **Bookmark state on list endpoints.** Only #4 returns `is_bookmarked`.                                                                                                                         | Every rail card's bookmark icon   | Rail cards render unbookmarked on every load; the icon flips locally after a #15 toggle and the truth appears on the course page. Adding the field to #1/#2/#3 fixes it.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| G-32     | Per-course **resources** outside the detail payload.                                                                                                                                           | Card "Additional resources" CTA   | #4 carries `ai_kit` and `exercise_file_url`; the catalog endpoints omit both, so a card has nothing to fetch. The course page opens them from the payload it already holds.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| G-33     | Course **sample** clip and course-navigation video.                                                                                                                                            | Hero "Sample" button, Resources   | #4 serves one trailer. `sample_link` and `navigation_link` are `null`; the sample button toasts "Trailer Not Found" and the navigation item never renders.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |

## Reporting

| #    | Gap                                                                                               | Where it shows                      | Current behaviour                                                                                                                                                              |
| ---- | ------------------------------------------------------------------------------------------------- | ----------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| G-25 | **CPE credits by calendar year.** CAIRA aggregates by level; there is no year dimension anywhere. | CPE tracker toolbar year selector   | Compliance reporting is year-based, so this is a real functional gap, not cosmetic.                                                                                            |
| G-26 | Bulk certificate download.                                                                        | CPE tracker toolbar                 | **Control removed** in the CAIRA reshape. Certificates are per-context embedded URLs opened from the row; there is no download endpoint and the LMS has no bulk button either. |
| G-27 | NASBA CSV template.                                                                               | CPE tracker toolbar                 | **Control removed** in the CAIRA reshape. No endpoint, and no counterpart in the shipped LMS.                                                                                  |
| G-28 | Country / profession scoping of content, pricing and copy.                                        | `/:country/:profession_type` prefix | No CAIRA endpoint takes either parameter. The prefix is now cosmetic and is not forwarded.                                                                                     |

## Parity gaps — found by auditing the shipped CAIRA LMS

Endpoints `CAIRA-LMS-2026` calls in production that the 50-endpoint API reference does not
document, so the audit above could not see them. All three are now in `caira.endpoints.ts`
under the `L*` numbering. Full inventory: `prompts/caira-lms-parity-binding.md` §2.

| #    | Gap                                                                                          | Where it shows                                        | Current behaviour                                                                                                                                |
| ---- | -------------------------------------------------------------------------------------------- | ----------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------ |
| G-34 | **Post-login gate** — maintenance window, pathway selection, onboarding (`web/app-status/`). | Post-login redirect, course page                      | Bound in P-P1 as `AppStatus`. Not yet wired to a guard, so nothing routes on the verdict. Polarity is inverted: all-true is the _happy_ learner. |
| G-35 | **Level tabs and FAQ content tree** (`caira/masterclass/levels-page/`).                      | Masterclass listing level tabs, FAQ accordion         | Bound in P-P2 as `LevelsPage`. Components not yet rewired, so the listing still renders its placeholder tabs.                                    |
| G-36 | **Salesforce activity relay** (`milesone-activity`), 4 events.                               | Login, chapter complete, feedback submit, badge claim | Bound in P-P7 as `CairaActivity`. No call sites yet — the four triggers land with their owning phases.                                           |

| G-39 | **No course-format discriminator on #4.** The Django API returned `podcast_format: 'Audio' \| 'Video'`; `Masterclass_Course_Detail` serves every type from one shape and reports only `hls_video_url`. | Podcast chapter player | `podcastFormat()` is held `null`, so the video layout renders for every podcast — correct for an HLS source, but the audio-first layout is unreachable. A `course_format` field on #4 would restore it. Chapter-level face of G-19. |
| G-38 | **Badge rows cannot navigate to their course.** #23 carries no `course_id`, slug or link on any badge entry. | CPE tracker table, all 6 row actions | Only the certificate / Credly action survives; the other five (`resume`, `exam`, `retake`, `feedback`, `view-details`) resolve to `'none'` and row titles render as plain text. The shipped CAIRA LMS's progress page has no navigation either, so this is the backend's shape, not a regression. Adding `course_id` to #23's badge entries would restore all five. |
| G-37 | **CPE tracker year selector has no backend dimension** — confirmed against the shipped LMS, whose tracker is level-based too. | `cpe-tracker` toolbar | `Cpe` exposes levels and badges, no year. The selector must be removed, not backfilled. Confirms G-25 is functional, not cosmetic. |

| G-40 | **No mode flag on the enrollment API.** #6 / #14 report the enrollment window but not whether it is a CPE or preview enrollment. | Chapter player | CPE mode is assumed for every chapter (`CPE_MODE_DEFAULT = true`) so no compliance rule is skipped. Backend ask: add the flag to #6's response and `ChapterProgress` reads it in one line. |

### Not a gap, a defect in the other client

`CAIRA-LMS-2026` authenticates through the **unprefixed** `login-with-phone-otp` /
`verify-otp` — the mobile twins (#39/#40), not the `web/*` routes (#34/#35). #39 returns the
generated OTP to any anonymous caller for any phone number with no throttle, and #40 has no
profile-completion gate. `caira.endpoints.ts` deliberately omits both and
`caira.endpoints.spec.ts` asserts they stay omitted. **Parity does not extend here** — this
is the one place miles-masterclass must diverge, and it already does.

## Backend defects worth raising

Found while binding; none is a frontend bug, and all are coded around.

| Where                   | Issue                                                                                                                                                                                                                                                                                                                                                           |
| ----------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `USP/authentication.py` | No `authenticate_header()` override, so every auth failure returns **403** instead of 401. Verified live.                                                                                                                                                                                                                                                       |
| #39                     | `communication_method=5` returns the generated OTP to **any anonymous caller, for any phone number, with no throttle**. Web binds #34 instead, which refuses it.                                                                                                                                                                                                |
| #23                     | `caira_badges.progress` numerator is the global grand total, so all three levels show the same numerator. #22 is correct — but the shipped LMS calls #23, so parity binds #23 and **corrects the numerator in `toLevelBadges`** (`progressCorrected`, rebuilt from the level's own badges). The raw value stays on `progress` so the divergence is inspectable. |
| #25                     | A non-UUID `level` 500s with a raw exception instead of returning 400.                                                                                                                                                                                                                                                                                          |
| #20, #24                | Internal failures are flattened to **400**, not 500.                                                                                                                                                                                                                                                                                                            |
| #26 vs #27              | Cache-prefix mismatch (`_V2_` vs non-`_V2_`) — claiming an alumni badge may not invalidate the read for 300 s.                                                                                                                                                                                                                                                  |
| #29                     | Cache read is gated on `not has_token`, which `IsAuthenticated` makes unreachable — written every request, never served.                                                                                                                                                                                                                                        |
| #43                     | The 404 and "Invalid question ID(s)" 400 return from **inside** `transaction.atomic()`, so the transaction still commits.                                                                                                                                                                                                                                       |
| views 1–18              | 500 handlers return `str(exc)` verbatim to the client. The frontend logs it and shows generic copy instead.                                                                                                                                                                                                                                                     |
